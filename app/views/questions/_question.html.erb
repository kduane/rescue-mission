
<h4><%= link_to "#{question.title}", question_path(question) %></h4>
<p><%= question.description %></p>

<tr>
  <td><%= link_to 'Answer' %></td>
  <td><%= link_to 'Edit', edit_question_path(question) %></td>
</tr>
